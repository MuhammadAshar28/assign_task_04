
def main():

    number = float(input("Type a number to see its square: "))
    
    # Calculate the square of the number
    square = number * number
    
    print(f"{number} squared is {square}")



if __name__ == '__main__':
    main()# This script calculates the square of a number entered by the user.
# It prompts the user to enter a number, calculates its square, and prints the result.